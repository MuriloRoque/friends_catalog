import React from 'react';
import header from '../styles/Header.module.css';

const Header = () => (
  <div className={header.header}>
    <h1 className={header.title}>FRIENDS CATALOG</h1>
  </div>
);

export default Header;
